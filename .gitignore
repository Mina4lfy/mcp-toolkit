# OAuth keys + per-server token files + LinkedIn cookies + Playwright profile
state/

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
dist/
build/

# Editor
.vscode/
.idea/
*.swp

# MCP Toolkit. No leading underscore: Google Console downloads these as
# client_secret_<id>.apps.googleusercontent.com.json, and _latest_client_json()
# scans the repo root for *client_secret*.json — the ignore must cover both.
*client_secret*.json

# Vendor libraries.
vendor/
